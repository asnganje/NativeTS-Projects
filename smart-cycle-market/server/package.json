{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the api built for our smart-cycle-market",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"tsnd --respawn -r tsconfig-paths/register --transpile-only ./src/index.ts",
    "build":"tsc && tsc-alias",
    "start":"node ./dist"
  },
  "author": "Nganje Suleiman",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "ts-node-dev": "^2.0.0"
  }
}
